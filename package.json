{
  "name": "dexdraw",
  "private": true,
  "version": "0.0.0",
  "description": "DexDraw — shared real-time whiteboard for business meetings",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "dev:client": "pnpm --filter @dexdraw/client-web dev",
    "dev:server": "pnpm --filter @dexdraw/server-api dev",
    "dev:tailnet": "./start-dev.sh",
    "share:client": "echo 'Run \"tailscale funnel 5173\" to accept public traffic.'",
    "share:server": "echo 'Run \"tailscale funnel 4000\" to accept public traffic.'",
    "test": "pnpm -r test",
    "typecheck": "tsc -b",
    "lint": "biome check .",
    "format": "biome format --write .",
    "lint:fix": "biome check --write ."
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}