{
  "name": "dexdraw",
  "private": true,
  "version": "0.0.0",
  "description": "dexDraw — shared real-time whiteboard for business meetings",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "share": "echo 'Run \"tailscale funnel 3000\" to accept public traffic, or use your Tailnet IP for private access.'",
    "test": "pnpm -r test",
    "typecheck": "tsc -b",
    "lint": "biome check .",
    "format": "biome format --write .",
    "lint:fix": "biome check --write ."
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}